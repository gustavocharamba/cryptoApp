import React from "react"

import { ContainerMenu } from "./menuStyled"

const Menu: React.FunctionComponent = (props) => {
    return(
        <ContainerMenu>

        </ContainerMenu>
    )
}

export default Menu